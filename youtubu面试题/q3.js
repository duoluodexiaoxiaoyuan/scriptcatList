/*
  write a function that accepts a String as an argument .
  The String is supposed to be HTML,but all the div elementsare 
  missing their closing tags (they have the < and >
  字符串是支持html标签的，但是所有的div标签丢了闭合标签 /
  The function's job is to find and close all the divs in theprovided HTML String
  The function should return the entire corrected string.
*/

function closeSecondDivs(string) {
  // 首先判断这个html有多少div标签，将div标签记录到一个数组中，如果是奇数div
  // 有问题需要重新梳理思路

}